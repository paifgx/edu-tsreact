export function IncidentList() {
  return (
    <section className="panel">
      <h3>TODO: IncidentList</h3>
      <p>
        Type the props, map the incidents, and add stable keys to render the list properly.
      </p>
    </section>
  );
}
